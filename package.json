{
  "name": "gh-pages",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "clean": "rm dist/bundle.js",
    "start": "parcel src/index.html",
    "build": "rm -rf dist && parcel build src/index.html",
    "deploy": "git push --force `git subtree split --prefix dist gh-pages-next`:gh-pages"
  },
  "dependencies": {
    "trianglify": "^4.0.0-next.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "@babel/core": "^7.9.6",
    "@babel/preset-env": "^7.9.6"
  }
}
